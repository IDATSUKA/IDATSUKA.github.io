# IDATSUKA デザイン定義書

idatsuka.com と、そこで販売するテンプレート製品のデザイン基準です。
**この文書の値は実装（`style.css`）から書き起こしたものです。** 実装を変えたら、この文書も同じコミットで更新してください。

- 対象: `*.html`（29ページ）、`style.css`、`js/`、`products/template-*`
- 最終更新: 2026-09-12

---

## 1. デザインの方針

ひとことで言うと **「暗く、静かで、精度が高い」**。

- **引き算**: 要素を足して説明するのではなく、余白と階層で語る
- **一点の色**: 画面のほとんどは無彩色。アイス・ブルー（`--accent`）は、ラベル・罫・数字・リンクのホバーなど「意味のある1点」にだけ置く
- **静かな動き**: 動きは常に減速（`--ease-out`）。跳ねない、光らせすぎない、待たせない
- **実装できる装飾だけ**: フィルムグレイン、微かなグロー、1pxの罫。画像で誤魔化さない
- **誠実さ**: 実在しない実績を書かない。想定案件は Spec と明示する（→ 10章）

参考にする質感は「プレミアムなショーリール系サイト」。ただしネオンには寄せず、この配色の静けさに翻訳する。

---

## 2. カラー

`style.css` の `:root` が唯一の定義元。**HEX の直書きは禁止**（`rgba()` で透明度を扱う場合を除く）。

| トークン | 値 | 用途 |
| --- | --- | --- |
| `--bg` | `#060607` | 背景（基準面） |
| `--bg2` | `#0b0c0e` | 一段持ち上げた面（カードのホバー、帯セクション） |
| `--bg3` | `#121317` | さらに上の面（入力欄など） |
| `--fg` | `#f4f5f7` | 本文・見出し |
| `--fg-dim` | `#c3c8d0` | 長文の本文（読み疲れを減らす） |
| `--muted` | `#70767f` | 補助テキスト・説明文 |
| `--dim` | `#3a3f47` | 番号・年号・最弱の文字 |
| `--accent` | `#9fd6ec` | アイス・ブルー。ラベル／強調／ホバー |
| `--accent-soft` | `rgba(159,214,236,.55)` | ホバー時の境界線 |
| `--border` | `rgba(255,255,255,.055)` | 通常の罫・グリッドの隙間 |
| `--border-strong` | `rgba(255,255,255,.12)` | ボタン・タグの境界線 |

**使い方のルール**

- アクセントを**面**に敷かない。`.btn-filled`（CTA 1つ）と `::selection` だけが例外
- グリッドの区切りは「線」ではなく `gap: 1px; background: var(--border)` で作る（`.works-grid` / `.skills-grid`）
- グローは `rgba(159,214,236,…)` を `box-shadow` / `text-shadow` / `drop-shadow` で。不透明度は **.3 以下**

---

## 3. タイポグラフィ

読み込むのは3書体まで。`Space Grotesk` / `Zen Kaku Gothic New` / `Space Mono`。

| 役割 | 指定 |
| --- | --- |
| 本文・見出し | `'Space Grotesk', 'Zen Kaku Gothic New', -apple-system, sans-serif` |
| ラベル・数値・UI | `var(--mono)` = `'Space Mono', 'SF Mono', ui-monospace, Menlo, monospace` |

**サイズ**（可変は `clamp()`。px 固定はしない）

| 用途 | サイズ | ウェイト・字間 |
| --- | --- | --- |
| ページ見出し `h1` | `clamp(3.4rem, 10vw, 8rem)` | 500 / `-.04em` |
| 作品ページ `h1` | `clamp(2.6rem, 7.5vw, 6rem)` | 500 / `-.04em` / `line-height: .9` |
| CTA 見出し | `clamp(1.9rem, 4.5vw, 3.2rem)` | 450 / `-.02em` |
| カード見出し | `1.25rem` | 500 / `-.015em` |
| 本文 | `.92rem` / `line-height: 2.05〜2.1` | 300〜400 |
| 説明文 | `.82rem` / `line-height: 1.85` | `--muted` |
| `.label` | `.58rem` | mono / `letter-spacing: .32em` / 大文字 / `--accent` |
| ナビ・ボタン | `.6rem` | mono / `.16〜.2em` / 大文字 |

**原則**

- 見出しは大きくするほど字間を詰める（`-.015em → -.04em`）。小さい文字ほど字間を開く（`.12em → .32em`）
- 日本語の本文は `line-height` を 1.8 以上。英字だけの UI ラベルは 1.5 前後
- `body` の既定 `line-height` は `1.8`
- `<em>` は斜体にせず、`font-style: normal` のままアクセント色にする（`.about-cta-title em`）

---

## 4. レイアウト

- コンテナ: `max-width: 1140px` / 左右パディング `3rem`（900px 以下で `1.5rem`）
- セクション縦: `8rem`（900px 以下で `5rem`）
- ブレークポイントは **900px と 480px の2つだけ**。増やさない
- グリッドは 2カラム（作品）／3カラム（スキル）が基本。900px 以下で 1カラム、スキル系は 2カラム
- 固定ヘッダー `nav` は `z-index: 200`、グレインのオーバーレイは `9999`

**モバイルの最低条件**: 390px 幅で横スクロールが発生しないこと（`document.documentElement.scrollWidth <= 390`）。

---

## 5. モーション

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--ease-out` | `cubic-bezier(.16,1,.3,1)` | 既定。表示・移動・拡大のほぼ全て |
| `--ease-soft` | `cubic-bezier(.4,0,.2,1)` | 往復する動き・状態の切替 |

- **リビール**: `.rv` に `opacity:0 / translateY(20px) / blur(3px)` → 交差監視で `.on` を付けて `.9s` で解除。兄弟要素は 90ms ずつ遅らせ、上限 420ms
- **ホバー**: 画像は `scale(1.04)`＋`brightness(1.08)` を `.7s`。文字はアクセント色へ `.3s`
- **持続時間の目安**: UI の反応 .3s / 要素の出現 .6〜.9s / 画像の変形 .7〜.8s
- **キャンバス演出**: 60fps を維持する。粒子ごとの `shadowBlur` は禁止（スプライトを事前描画して貼る）。画面外・非表示タブでは `requestAnimationFrame` を止める
- **`prefers-reduced-motion: reduce` は必須対応**。アニメーションと遷移を `.01ms` に、`.rv` は最初から可視、グレインとカーソル演出は停止

---

## 6. コンポーネント

**既存のクラス名は変更しない。29ページが依存しています。** 新しい見た目が必要なときは、既存クラスに修飾子を足すか、新しい名前を追加する。

| クラス | 用途 |
| --- | --- |
| `.label` | セクションの通し番号つきラベル（例: `01 — Products`） |
| `.container` / `section` | 基本の囲みと縦リズム |
| `.btn` / `.btn-filled` | 通常ボタン（枠線・ホバーで下から塗りが上がる）／ CTA（1画面に1つまで） |
| `.rv` | スクロールで現れる要素 |
| `.divider` | 中央が光る 1px の区切り |
| `.page-header` | 各ページの大見出し（ラベル＋`h1`＋説明） |
| `.work-card` / `.work-card-home` | 作品カード（サムネ 16:9・カテゴリ・題・説明） |
| `.skills-grid` / `.skill-item` | 3カラムの一覧。Store の商品カードもこれを流用 |
| `.timeline-home .tl-item` | 年表の行（年 / 役割 / 説明） |
| `.spec-note` / `.spec-note-wrap` | **架空案件の注記**（頭に `SPEC` バッジが自動で付く） |
| `.proc-rail` / `.proc-step` | 番号つきの工程レール |
| `.filter-bar` / `.filter-btn` | 絞り込み。**見た目だけでなく実際に絞り込むこと** |
| `.project-visual` / `.project-gallery-full` | 作品ページの大判ビジュアル（21:9、900px 以下で 16:9） |
| `.tmpl-row` / `.tmpl-cover` | テンプレート紹介の並び（`biz.html`） |

**画像の比率**: サムネ `16/9`、人物写真 `3/4`（900px 以下で `16/9`）、作品ヒーロー `21/9`、商品カバー `16/9`。

---

## 7. ページの型

すべてのページが共通で持つもの:

1. `<nav>` ＋ `.mobile-menu`（6項目: Profile / Play / Business / Store / Blog / Contact、現在地に `.active`）
2. `.page-header`（トップページは `.hero-*`）
3. 本文セクション（`.label` で番号を振る）
4. `.about-cta`（次の行動への導線）
5. `<footer>`（`© 2025 IDATSUKA` ＋ 同じ6リンク）
6. `<head>`: `title` / `description` / `og:title` / `og:description` / `canonical` / SVG ファビコン
7. 末尾に `menuToggle` の開閉と `.rv` の交差監視

---

## 8. 画像・メディア

- 形式: 図版・カバーは **SVG**、写真・キービジュアルは **JPEG**（品質 3〜4 相当）、映像は **mp4(H.264) ＋ webm(VP9)** の2本立て＋ポスター画像
- 外部CDNの画像は使わない。ページ内 SVG かリポジトリ内のファイルのみ
- 映像は `autoplay muted loop playsinline` ＋ `poster`。**ファイルが無くても CSS のグラデーションで成立する**ように書く
- ループ映像は継ぎ目が見えないこと（開始フレームと終了フレームを並べて確認する）
- 装飾画像は `aria-hidden="true"`、意味を持つ画像には `alt` を書く

---

## 9. アクセシビリティ

- 本文と背景のコントラスト比 **4.5:1 以上**（`--muted` を小さな文字で長文に使わない）
- タップ領域は **44px 以上**
- `:focus-visible` は `1px solid var(--accent)` / `outline-offset: 3px`。消さない
- 開閉する UI には `aria-expanded`、JS なしでも開ける箇所は `<details>` を使う
- `prefers-reduced-motion` 対応（→ 5章）
- 見出しレベルを飛ばさない（`h1` → `h2` → `h3`）

---

## 10. 表記とコンプライアンス

- **架空の案件には必ず `.spec-note` を付ける**（`biz.html` / `work-0X.html`）。「Spec Works = 実在のクライアント・実績ではない」と明記する
- 実績・受賞歴・口コミ・数値は**確認できる事実のみ**。根拠のない「No.1」「日本一」「大人気」は書かない（景品表示法）
- テンプレートのデモ内容は `◯◯` `△△` `¥◯,◯◯◯` のプレースホルダーにする。実在しそうな企業名・人名を置かない
- 通信販売を行うページには「特定商取引法に基づく表記」を用意する
- 他者の著作物（キャラクター・写真・フォント）を同梱しない

---

## 11. テンプレート製品の定義

`products/template-*` として販売する HTML テンプレートの共通規約。
**本体サイトの配色は継承しません。** 製品ごとに世界観を持たせ、その中で下記の作法を守ります。

| 製品 | 世界観 | 主色 |
| --- | --- | --- |
| ZEN | ミニマルなポートフォリオ | ダーク／ライト |
| RAY | 1オファーのLP | 光のビーム |
| PULSE | 映像的・高級感のあるLP | 深度とガラス |
| NOREN | 老舗（明治〜昭和創業） | 和紙 `#f4efe6` / 墨 `#1c1a17` / 朱 `#b23a2a` / 金 `#b8a36b` |
| KIRA | 劇場版アニメの作品公式サイト | 夜空 `#0b0a14` / 金 `#e8c27a` / 夕焼けの桃・空色 |

**必須要件**

1. **1ファイル完結**（HTML / CSS / JS 同梱）。外部ライブラリ・ビルドツールなし。映像などの重い素材のみ `assets/` に置く
2. 冒頭に日本語のヘッダーコメント: `■ カスタマイズ手順` / `■ 収録しているもの` / `■ 掲載内容について（景品表示法）` / ライセンス行
3. ライセンス表記は固定: **`個人・商用利用可 / テンプレートとしての再販・再配布は不可`**
4. 編集箇所すべてに `EDIT:` コメント。「最初に差し替える5〜6か所」を README の表にする
5. **2テーマ**を `<html data-theme>` で切替。`localStorage`（キーは製品名） に保存し、`theme-color` メタを同期。描画前に適用してちらつきを防ぐ
6. `prefers-reduced-motion` 対応、390px で横スクロールなし、コンソールエラーゼロ
7. `<head>` に OGP / canonical / theme-color / SVG ファビコン
8. README は同じ構成: 使い方 → 最初に差し替える◯か所 → テーマ → 特徴 → セクション構成 → 掲載内容についてのお願い → ライセンス → サポート → 変更履歴
9. 配布は `products/idatsuka-template-<name>-v<x.y>.zip`。**旧バージョンの zip は消さない**
10. 価格は `¥2,980`（応援価格・無料版あり）。Store カードの決済リンクは `store.html` の `PAY_LINKS` に追加する

**バージョンの付け方**: 見た目や機能の追加＝マイナー（v1.0 → v1.1）、作り直し＝メジャー（v1.x → v2.0）。上げたら zip・README・`store.html` のカード・カバー画像の版数表記をすべて揃える。

---

## 12. 公開前チェックリスト

- [ ] 1320px と 390px の両方を実機幅で描画して目視した
- [ ] 横スクロールが出ない（`scrollWidth == clientWidth`）
- [ ] コンソールエラーなし、404 のリンク・画像なし
- [ ] `prefers-reduced-motion` で全要素が見える
- [ ] ナビ／フッターの6項目と `.active` が正しい
- [ ] `title` / `description` / `og:` / `canonical` を書き換えた
- [ ] 架空の内容に `.spec-note` かプレースホルダーが付いている
- [ ] 新しい色を HEX 直書きしていない（トークン経由）
- [ ] テンプレートなら zip を作り直し、Store のカード・版数・決済リンクを更新した

---

## 13. 作業の進め方

- 変更は必ず `style.css` と `index.html` を読んでから。**既存の仕組みを拡張し、上から新しい書き方を被せない**
- 見た目の判断はコードではなくスクリーンショットで行う（ヘッドレス Chromium で描画して確認する）
- 同じ修正を多数のページに適用する作業と、公開前の全体監査は専用エージェント（`bulk-edit` / `site-qa`）に任せる
- 判断基準に迷ったら、この文書の1章に戻る
