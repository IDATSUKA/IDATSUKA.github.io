# Claude Code への引き継ぎガイド

## 渡し方（3ステップ）

1. `idatsuka-credits.zip` を解凍し、中身をプロジェクトフォルダに置く
   （既存のNext.jsプロジェクトがあるなら、ファイルをそこへ統合する）
2. そのフォルダで Claude Code を起動する
3. 下の「最初のプロンプト」をコピペして送る

`CLAUDE.md` がプロジェクト直下にあるので、Claude Code はそれを読んで文脈を掴む。

---

## 最初のプロンプト（これをコピペ）

```
このプロジェクトの CLAUDE.md をまず読んで、現状を把握してください。

IDATSUKA という AI画像生成プラットフォームのフェーズ0のコードが入っています。
「サインイン → クレジット購入 → 生成」の一周は実装済みで、これから本番デプロイと
各サービスの配線をします。

今日やりたいのは、まずローカルで動く状態にすることです。順番に進めてください:

1. npm install して、npm run build が通るか確認。型エラーやimportエラーがあれば直す。
2. lib/higgsfield.ts と app/api/webhooks/higgsfield/route.ts は公式ドキュメント
   ベースの推定なので、実際のHiggsfield APIに合わせて要確認。まずは現状の実装で
   ビルドが通ることを優先し、実APIの現物合わせは私のキーが用意できてから一緒にやりましょう。
3. 詰まったら、私（オーナー）がどのサービスにログインして何を取得すべきか、
   具体的に指示してください。ログインやキー入力は私がやります。

各サービスのAPIキーはまだ設定していません。.env.local のテンプレートを作って、
私が何を埋めればいいか教えてください。
```

---

## このあとの流れ（Claude Codeと進める順）

1. ローカルビルドを通す
2. Supabase: プロジェクト作成 → migration適用 → キー取得 → .env.local へ
3. Higgsfield: 実APIの現物合わせ（エンドポイント/Webhook/原価）
4. Stripe: Price作成 → Webhook → ローカル決済テスト
5. Vercelデプロイ → app.idatsuka.com 追加 → 環境変数登録
6. 既存の IDATSUKA.com に「ツールを開く」導線を1つ足す

各ステップで「オーナーがやる操作」と「Claude Codeがやる操作」が分かれる。
ログイン・キー入力・デプロイ・DNS変更はオーナー、コードとローカル作業はClaude Code。
